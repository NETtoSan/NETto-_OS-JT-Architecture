class permParsing{
  constructor(bot){
    this.validiator = []
    this.bot
  }

  async check(message,args,bot,app){
    this.bot = bot
    const esfx = async () =>{
      const permexec = await this.permparse(app,a,b)
      if(permexec){
        switch(permexec){
          
        }
      }
    }
  }
}



module.exports = permParsing
